# Kerala Tourism AI Chatbot - Requirements
# Install with: pip install -r requirements.txt

# ---- Web Framework ----
Flask==3.0.0                  # Main web framework (routing, templates, sessions)
Werkzeug==3.0.1               # Flask dependency (request/response handling)

# ---- NLP / AI Libraries ----
scikit-learn==1.4.0           # TF-IDF vectorization + cosine similarity
numpy==1.26.3                 # Numerical operations (array math)

# ---- Fuzzy Matching (Spell Correction) ----
fuzzywuzzy==0.18.0            # Fuzzy string matching library
python-Levenshtein==0.23.0    # C extension to speed up fuzzywuzzy (optional but faster)

# ---- Utilities ----
Jinja2==3.1.3                 # HTML template engine (Flask uses this)
